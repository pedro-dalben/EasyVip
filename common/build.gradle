plugins {
    id 'net.neoforged.moddev' version '2.0.141'
}

base {
    archivesName = "${mod_id}-common"
}

neoForge {
    version = project.neo_version

    parchment {
        mappingsVersion = project.parchment_mappings_version
        minecraftVersion = project.parchment_minecraft_version
    }
}

configurations {
    testCompileOnly.extendsFrom compileOnly
    testCompileClasspath.extendsFrom compileClasspath
    testRuntimeClasspath.extendsFrom runtimeClasspath
}

repositories {
    maven {
        name = 'LuckPerms'
        url = 'https://repo.luckperms.net/'
    }
}

dependencies {
    compileOnly "curse.maven:ftb-ranks-314905:6431744"
    compileOnly "curse.maven:ftb-library-404465:7746959"
    compileOnly "net.luckperms:api:5.4"

    compileOnly 'com.mysql:mysql-connector-j:8.0.33'
    compileOnly 'com.zaxxer:HikariCP:5.1.0'

    testImplementation platform('org.junit:junit-bom:5.10.2')
    testImplementation 'org.junit.jupiter:junit-jupiter'
    testImplementation 'org.mockito:mockito-inline:5.2.0'
    testRuntimeOnly 'org.junit.platform:junit-platform-launcher'
}
